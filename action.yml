name: 'Set up FORM'
description: 'Install FORM and add it to the PATH'
author: 'Takahiro Ueda'
branding:
  icon: 'chevrons-right'
  color: 'blue'
inputs:
  form-version:
    description: 'The FORM version to download and use. Example: 4.3.0'
    default: '4.3.0'
runs:
  using: 'node12'
  main: 'dist/index.js'
